server:
  port: 0
  
hystrix:
  command:
    default:
      execution:
        isolation:
          strategy: THREAD
          thread:
            timeoutInMilliseconds: 60000
            
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
